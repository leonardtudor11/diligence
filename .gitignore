.venv/
__pycache__/
*.pyc
.env
.env.local
*.egg-info/
.pytest_cache/
.mypy_cache/

# Cached primary sources — large, redownloadable
# Entire data/ tree is per-ticker JSON + audio caches built by services/ingest.py.
# Re-derivable; never commit. .env keeps API keys, .env.example keeps the schema.
data/
*.mp3
*.wav
*.m4a

# Local notes
notes/
scratch/
